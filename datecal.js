function DateCal() {
    const dayel = document.getElementById("day");
    const monthel = document.getElementById("month");
    const yearel = document.getElementById("year");
    if (yearel % 4 === 0 && yearel % 100 !== 0 || yearel % 400 === 0) {
        
    }
}